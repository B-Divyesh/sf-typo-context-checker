[
  {
    "id": "workspace-context",
    "claim": "The VS Code extension checks a newly introduced token against permitted local repository vocabulary.",
    "where": "landing page, README, VS Code package",
    "test": "npm test -- -t @claim:workspace-context",
    "sandbox": "fixture workspace with a trusted database_url token and a newly introduced databse_url token"
  },
  {
    "id": "sensitive-defaults",
    "claim": "Environment files, secret directories, and PEM files are excluded by default.",
    "where": "landing page, privacy page, README, extension settings",
    "test": "npm test -- -t @claim:sensitive-defaults",
    "sandbox": "workspace-relative root and nested sensitive paths"
  },
  {
    "id": "demo-local-match",
    "claim": "The sample checker finds and explains near-match code tokens.",
    "where": "landing page and /demo/",
    "test": "npm run test:e2e -- --grep @claim:demo-local-match",
    "sandbox": "fresh browser context using only the sample repository vocabulary"
  },
  {
    "id": "site-local-only",
    "claim": "The site checker sends entered text to no other origin.",
    "where": "landing page, /demo/, privacy page",
    "test": "npm run test:e2e -- --grep @claim:site-local-only",
    "sandbox": "fresh browser context recording every request while editing and checking sample data"
  },
  {
    "id": "extension-local-only",
    "claim": "The extensions keep their settings local and do not upload source or call analytics and model APIs.",
    "where": "landing page, privacy page, README",
    "test": "npm test -- -t @claim:extension-local-only",
    "sandbox": "static scan of every VS Code and Chromium runtime entry point plus the browser host permission"
  },
  {
    "id": "vscode-quick-fix-undo-dismiss",
    "claim": "VS Code Quick Fix can use the existing token with native Undo or dismiss that exact comparison.",
    "where": "landing page, README",
    "test": "npm run test:vscode",
    "sandbox": "packaged VSIX in an isolated workspace; type a near-match, apply Quick Fix, run Undo, then dismiss its exact pair"
  },
  {
    "id": "github-diff-checking",
    "claim": "The optional Chromium companion checks visible GitHub pull request, commit, and compare pages.",
    "where": "landing page, README, privacy page",
    "test": "npm run test:extension",
    "sandbox": "unpacked MV3 extension and intercepted GitHub fixtures for one pull request, commit, and compare URL"
  },
  {
    "id": "workspace-file-limit",
    "claim": "The VS Code extension indexes up to 300 permitted local files by default.",
    "where": "README, VS Code settings",
    "test": "npm test -- -t @claim:workspace-file-limit",
    "sandbox": "read the packaged VS Code configuration schema"
  },
  {
    "id": "vscode-version",
    "claim": "Context Check supports VS Code 1.85 and later.",
    "where": "landing page, VS Code package",
    "test": "npm test -- -t @claim:vscode-version",
    "sandbox": "read the packaged VS Code engine range"
  },
  {
    "id": "no-paid-gate",
    "claim": "Context Check has no account or payment gate.",
    "where": "landing page, README, privacy page",
    "test": "npm test -- -t @claim:no-paid-gate",
    "sandbox": "static scan of public product and package files for account and billing integrations"
  },
  {
    "id": "demo-ephemeral",
    "claim": "The sample checker saves nothing after you close the page.",
    "where": "landing page, demo page, README",
    "test": "npm run test:e2e -- --grep @claim:demo-ephemeral",
    "sandbox": "fresh browser context runs the sample checker and asserts no cookies, localStorage, or sessionStorage entries"
  }
]
